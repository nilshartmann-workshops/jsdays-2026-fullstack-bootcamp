import { Component } from '@angular/core';

@Component({
  selector: 'app-book-card',
  templateUrl: './book-card.html',
})
export class BookCard {
  // todo:
  //   - Ein Input-Signal für das Buch definieren:
  //     readonly book = input.required<Book>();
  //   - Im Template die Eigenschaften von book() ausgeben
  //     (Titel, Jahr, ISBN, Seiten)
  //   - Die CSS-Klasse "Card" verwenden
}
