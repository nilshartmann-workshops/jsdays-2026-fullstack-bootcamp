# Todo: Erweitere den "type Author" mit:
#  firstName, lastName (jeweils vom Typ String)
#
type Author {
  id: ID!
}

# Todo 2:
#  Erweitere den Query Type:
#  extend type Query {
#     ...
#  }
#   - es soll zwei Felder geben:
#     - authors (ohne Argumente) liefert eine Liste von Author-Objekten
#     - authorById (Argument: "id: ID!"), liefert einen Author zurück (oder null)
#
